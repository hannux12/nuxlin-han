apt install postfix dovecot-imapd dovecot-pop3d
dpkg-reconfigure postfix
        Internet Site
        dimas.com (your domain)
        dimas (username)
        mail.dimas.com, dimas.com, localhost, localhost.localdomain

nano /etc/postfix/main.cf

myhostname = mail.dimas.com
mydomain = dimas.com
myorigin = /etc/mailname
inet_interfaces = all
inet_protocols = all
mydestination = $myhostname, localhost.$mydomain, localhost, $mydomain
mynetworks = 127.0.0.0/8 192.168.10.0/24
home_mailbox = Maildir/

nano /etc/dovecot/dovecot.conf
listen = *, ::

nano /etc/dovecot/conf.d/10-mail.conf
 mail_location = maildir:~/Maildir

systemctl restart postfix devecot

su - dimas
maildirmake.dovecot ~/Maildir

nano /etc/bind/db.domain

mail    IN  A   192.168.10.12
@       IN  MX  10 mail.dimas.com.


mail budi@dimas.com

cd ~/Maildir/new
cat * 
